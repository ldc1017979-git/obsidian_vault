# 统一启动指令：全新需求验证

先确认Codex当前项目目录是完整解压后的根目录，能同时看到`workflow/AGENTS.md`、`workflow/19-端到端阶段门禁总表.md`、`workflow/20-事实契约、单一事实源与五维状态规范.md`和`本次任务/`。这些工作流路径缺失时才停止并让负责人补发完整包。本人任务卡尚未填写不属于发包缺失；Codex应按下面指令帮助产品经理自行建立任务卡。`workflow/21_新需求验证包/01-个人任务卡-空白模板.md`标记为`TEMPLATE_ONLY`，不是本次任务输入：

```text
这是一次产品经理工作流V3的全新需求验证，不是旧案例回归，也不是只修HTML。

请完整读取当前项目目录中的workflow/AGENTS.md、workflow/19号阶段门禁和workflow/20号事实/状态规范，再按阶段读取workflow中的其余文件。

先检查`本次任务/`中是否存在名称符合`个人任务卡_姓名_需求名称.md`的已填写任务卡：
- 如果存在，逐个读取该任务卡和`本次任务/原始资料/`中的文件，不要只看目录或文件名。
- 如果不存在或必填内容为空，不要让负责人指定任务，也不要先发完整问卷。只向我收集三项最小信息：我的姓名、我自行选择的当前全新需求名称、业务方原话或原始资料路径。确认该需求没有使用本工作流处理过后，基于根目录`本次任务/01-个人任务卡_请本人填写并重命名.md`生成`本次任务/个人任务卡_姓名_需求名称.md`，再进入阶段0。

所有工作流规则路径都以当前项目目录下的workflow/为根目录，当前需求事实路径都以本次任务/为根目录。不要引用负责人电脑、其他产品经理电脑或历史项目中的绝对路径。

本次只读取`本次任务/`中名称符合`个人任务卡_姓名_需求名称.md`且必填内容已填写的文件作为当前任务卡。不要把根目录待填写卡或workflow内的空白任务卡、总结、反馈模板当成已填写事实；需要填写workflow模板时先复制到本次个人项目输出目录，禁止改写workflow母版。

硬性要求：
1. 先核对个人任务卡、原始资料、目录名、现有系统证据是否冲突；冲突未关闭不得进入正式PRD。
2. 明确本轮项目终点，并据此决定执行到哪个阶段。
3. 客观事实从资料抽取，不用选择题诱导；每阶段遵守决策预算。
4. 原型前必须形成名词表、截图逐元素台账、页面契约、字段契约、状态×动作×角色×关联影响契约，并给我确认。
5. 评审辅助动作只能放开发/调试模式，不得混入正式业务按钮。
6. 每次版本升级同步当前阶段、当前结论、当前版本、未覆盖项、待确认项和规则/AC统计，并扫描旧版本残留。
7. 交付时分别报告产品确认、内部原型QA、研发准入、真实系统UAT和整体项目五维状态。
8. 测试数据必须虚构或脱敏，完整格式敏感数据默认不允许。

请直接从阶段0“新需求隔离与事实冲突检查”开始。不要让我先填写完整问卷。
```
