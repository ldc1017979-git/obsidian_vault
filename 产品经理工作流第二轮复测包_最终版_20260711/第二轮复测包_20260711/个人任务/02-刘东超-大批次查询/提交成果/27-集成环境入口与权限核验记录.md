# 大批次查询｜集成环境入口与权限核验记录

## 当前结论

- 核验日期：2026-07-15
- 测试账号：`txc_admin_6`
- 目标批次：`GD12A015`
- 登录：PASS
- 大批次查询入口：未发现
- 性能验收：**BLOCKED / 未覆盖**

## 客观证据

1. 更新凭据后登录接口返回`code=1，登录成功`，随后进入数智工厂工作台。
2. 工作台显示账号名称“测试账号6”。
3. 打开“生产管理”和“过程管理”后，页面文字中不存在“大批次查询”。
4. 实际角色菜单接口`/apios/home/getRoleList`返回HTTP 200、业务码1；在完整角色菜单数据中搜索“大批次查询”，结果为0。
5. 角色菜单数据中仅发现三个其他批次相关功能：`批次明细`、`原料批次库存查询`、`原厂批次编码规则`，均不是本项目的大批次查询。
6. 因无功能入口，未对`GD12A015`发起查询，不能记录结果条数和10秒性能。

## 环境附带问题

工作台和菜单操作过程中浏览器控制台出现多次空对象属性读取错误，包括：

- `Cannot read properties of null (reading 'getAttribute')`
- `Cannot read properties of undefined (reading 'type')`

这些错误发生在现有系统工作台环境，尚不能归因于未部署的大批次查询功能，作为环境问题单独保留。

## 证据位置

`提交成果/测试证据/集成环境/`

- `01-生产管理无大批次查询入口.png`
- `integration-check.json`

证据文件不包含明文密码。

## QA-P1-04状态

继续OPEN。当前阻塞已从“登录凭据不可用”更新为“账号无功能入口或环境尚未部署功能”。

## 下一步

需要以下任一条件：

1. 为`txc_admin_6`开通“大批次查询”菜单和路由权限；或
2. 提供一个已拥有该功能权限的账号；或
3. 提供该功能在测试环境中的直接路由，并确认已部署。

入口可用后，再以`GD12A015`执行真实条数、完整性和10秒性能验收。
