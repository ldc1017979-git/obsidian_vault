## 文件读写限制

所有读取和输出都必须发生在当前工作目录下，由用户指定的子文件夹内。

不要写入当前目录根部，除非用户明确要求。

不要访问当前工作目录之外的路径。

如果用户没有指定输入或输出子文件夹，应先询问用户。

## 删除限制

禁止在本机器上批量删除文件或目录。

不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf
- 1111
- 